#include "string.h"
#include <iostream>

using namespace std;

int main() {

  // char string[10] = {'a', 'h', 'm', 'e', 'd', '\0'};
  // int length = str_len(string);
  // cout << length << endl;

  // char string[10] = {'A', 'H', 'm', 'e', 'd', '\0'};
  // tolower(string);
  // cout << string << endl;

  // char string[10] = {'a', 'h', 'm', 'e', 'd', '\0'};
  // tocapital(string);
  // cout << string << endl;

  // char str1[10] = {'a', 'h', 'm', 'e', 'd', '\0'};
  // char str2[10] = {'a', 'h', 'm', 'e', 'd', '\0'};
  // int status = str_cmp(str1, str2);
  // cout << status << endl;

  // char str1[11] = {'a', 'h', 'm', 'e', 'd', '\0'};
  // char str2[10] = {'a', 'h', 'm', 'e', 'd', '\0'};
  // int isConcated = str_concat(str1, str2, sizeof(str1) / sizeof(str1[0]));
  // cout << isConcated << endl;
  // cout << str1 << endl;

  // char str1[11] = {'a', 'h', 'm', 'e', 'd', '\0'};
  // char str2[10] = {'M', 'o', 'h', 'a', 'm', 'e', 'd', '\0'};
  // int isCoped = str_cpy(str1, str2, sizeof(str1) / sizeof(str1[0]));
  // cout << isCoped << endl;
  // cout << str1 << endl;

  return 0;
}